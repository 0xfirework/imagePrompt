## 描述

请用中文简要说明此 PR 的目的与变更内容。

## 关联问题

Fixes #

## 截图 / GIF（如涉及 UI）

拖拽或粘贴截图到这里。

## 变更类型

- [ ] feat（新功能）
- [ ] fix（缺陷修复）
- [ ] docs（文档变更）
- [ ] refactor（重构，无功能改动）
- [ ] chore（构建/依赖/脚本等杂项）

## 自检清单

- [ ] 我已阅读并遵守《AGENTS.md》，所有说明、讨论与注释均使用中文。
- [ ] 本地通过 `bun run lint`、`bun run typecheck`、`bun run build`。
- [ ] 如涉及数据库，已在 `.env.local` 配置并执行 `bun db:push`（如需）。
- [ ] 如涉及 UI 变更，已附上截图/GIF。
- [ ] 如行为变更，已更新相关文档（README/AGENTS/代码注释）。

## 验证步骤（可选）

列出审阅者可复现与验证的步骤，例如：

1. `bun run dev:web`
2. 访问 `http://localhost:3000`
3. 打开 xx 页面，验证 yy 行为

